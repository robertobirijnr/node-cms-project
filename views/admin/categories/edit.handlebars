<h1> Edit</h1>

<div class="row">
    <div class="col-sm-4">
        <form action="/admin/categories/edit/{{category.id}}?_method=PUT" method="POST">
          <div class="form-group">
            <label for="name">Name</label>
            <input type="text" value="{{category.name}}" placeholder="Enter title" name="name" class="form-control"> 
        </div>
         <button class="btn btn-primary">Create Categories</button>
        </form>
      
    </div>
</div>